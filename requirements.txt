# Eye-gaze + voice cursor control
opencv-python>=4.8.0
mediapipe>=0.10.0
pyautogui>=0.9.54
SpeechRecognition>=3.10.0
numpy>=1.24.0

# Optional: microphone (SpeechRecognition needs it for live mic)
# pip install pyaudio
# On macOS: brew install portaudio && pip install pyaudio
