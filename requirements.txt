# Hand-tracking cursor (gaze_voice_cursor.py)
opencv-python>=4.8.0
mediapipe>=0.10.0
pyautogui>=0.9.54
numpy>=1.24.0

# Microphone (optional, for tongue-click to click)
# pip install pyaudio
# macOS: brew install portaudio && pip install pyaudio
# Linux: apt install portaudio19-dev && pip install pyaudio
